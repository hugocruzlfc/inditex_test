import rowRouter from "./row.controller.js";
import templateRouter from "./template.controller.js";
import productRouter from "./product.controller.js";

const Controllers = {
  rowRouter,
  templateRouter,
  productRouter,
};

export default Controllers;
